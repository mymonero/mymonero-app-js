"use strict"

class WalletsList
{
	constructor(options, renderer_context)
	{
		const self = this
		//
		self.options = options
		self.renderer_context = renderer_context
		console.log("Constructor for wallets list!")
	}
}
module.exports = WalletsList
